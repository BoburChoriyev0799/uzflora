- if @plants.present?
  %ol
    - @plants.each do |plant|
      %li
        = link_to safe_join([plant_display_name_html(plant, I18n.locale), ' | ', plant_sci_name_html(plant)]), "#", data: {id: plant.id}
