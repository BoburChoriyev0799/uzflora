.row
  %ol
    -@species.each do |plant|
      %li
        =link_to plant_path(plant) do
          %em= plant.species_sci
          - if plant.species_uz.present?
            %span= " (#{plant.species_uz})"
